# Estado remoto: se recomienda S3 con DynamoDB para locking en equipo.
# configurado en backend.tf (no se incluye por defecto para no forzar credenciales).
# bucket  = "cloud-native-005d-terraform-state"
# key     = "proyecto_estructura_v1/terraform.tfstate"
# region  = "us-east-1"
